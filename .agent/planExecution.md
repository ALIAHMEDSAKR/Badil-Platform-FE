Phase 1: Scaffolding & Types. Generate all TypeScript interfaces directly from the OpenAPI Swagger spec and set up the routing/folder structure.

Phase 2: API Client & State. Build the Axios/Fetch interceptors and the global state store.

Phase 3: Component Library. Build the atomic UI components (buttons, inputs, cards) matching the dark-mode aesthetic in your provided UI images.

Phase 4: Page Assembly & Integration. Stitch the components together into the main views (Auth, Dashboard, Marketplace) and wire them to the API.